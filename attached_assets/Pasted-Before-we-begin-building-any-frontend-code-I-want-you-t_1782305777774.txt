Before we begin building any frontend code, I want you to perform a thorough health-check on our existing backend workspace to verify that everything is complete and fully operational. 

Please slow down, inspect the workspace, and verify the setup across these specific files:
1. Verify that `gateway-node/server.js` and `gateway-node/socketHandlers.js` have the full event routing loops implemented cleanly (handling MongoDB persistence and connection fallback pipelines).
2. Verify that `gateway-node/models/Ticket.js` holds our Mongoose schema rules correctly (enforcing open → handoff → closed lifecycle states).
3. Verify that `ai-python/main.py` is configured properly to map incoming payloads, parse intent tokens with the true Google Gemini API client integration, and correctly hand off session states using a triggerHandoff: true structure.

Once you have verified the codebase, provide a concise summary outlining:
- Exactly how the backend microservices and Node Gateway are currently built.
- The real-time messaging data workflow (how data moves from a customer message to MongoDB, forwards through HTTP POST to the Gemini engine, handles automatic handoffs on keyword or sentiment mismatch, and transitions into a pure agent room).

Next Steps: Frontend Blueprint Mapping
With the backend checks verified, let’s move our focus into the client-react/ directory on the frontend branch. Do not modify the root README.md file right now.

Instead of writing code files directly yet, provide a detailed frontend architecture blueprint showing exactly how you intend to wire up our real-time interactive layout:

1. Global Connection Layer (client-react/src/context/SocketContext.jsx)
   - Detail how you will structure a clean, resilient single-connection instance to our Node Gateway service (Port 5000) using socket.io-client.
   - Show how connection events and the socket state will be exposed universally via global context.

2. Customer Chat Bubble Widget (client-react/src/components/ChatWidget.jsx)
   - Detail the UI state layout of an absolute-positioned floating widget fixed to the bottom right.
   - Explain the exact event parameters for emitting customer:message and catching real-time streaming tokens or agent:reply answers.

3. Human Agent Live Dashboard View (client-react/src/components/AgentDashboard.jsx)
   - Detail your layout plans for a dual-pane setup: tracking the active ticket pipeline and managing the active live workspace chat window.
   - Map out the exact step-by-step logic explaining how the dashboard intercepts a socket handoff event notification, switches rooms using agent:join, and enables symmetrical bidirectional messaging via agent:message.
